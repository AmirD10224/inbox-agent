// Tailwind v4 ships its own prefixer via Lightning CSS, no separate
// `autoprefixer` plugin needed. Keep this file minimal.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
